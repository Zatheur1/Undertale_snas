/// @description Insert description here
// You can write your code in this editor
var gg;
if (camera.x >= 0)
    x = (200 + floor((camera.x - (camera.x * scrollspeed))))
gg = (room_width - (camera.width / camera.scale_x))
if (camera.x >= gg)
    x = (200 + floor((gg - (gg * scrollspeed))))